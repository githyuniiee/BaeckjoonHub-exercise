# -- 코드를 입력하세요

            
            



SELECT H.FLAVOR
FROM FIRST_HALF H
INNER JOIN (
    SELECT FLAVOR, SUM(TOTAL_ORDER) AS SUM
FROM JULY
GROUP BY FLAVOR 
) J
ON H.FLAVOR = J.FLAVOR
ORDER BY H.TOTAL_ORDER + J.SUM DESC
LIMIT 3;
